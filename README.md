# Text-to-image-using-Stable-diffusion


# Stable Diffusion Inference

This repository contains a Python script for running inference with the Stable Diffusion model using the `mediapy` and `torch` libraries.

## Installation

Make sure you have the required dependencies installed:

```bash
pip install mediapy torch diffusers
